// GlassFish
glassfish {
    glassfishDir = "D:/Java/payara41/glassfish"
    moduleDir = "${glassfishDir}/modules"
    asadmin = "${glassfishDir}/bin/asadmin.bat"
    domainName = "domain1"
    hostName = "localhost"
    httpPort = "8080"
    adminPort = "4848"
    adminUsername = "admin"
}

// MySQL
mysql {
    homeDir = "D:/DB/MySQL/MySQL Server 5.5"
    driverDir = "D:/Java/mysql-connector-java-5.1.7"
    driverJar = "mysql-connector-java-5.1.7-bin.jar"
    driverClass = "com.mysql.jdbc.Driver"
    datasourceClass = "com.mysql.jdbc.jdbc2.optional.MysqlConnectionPoolDataSource"
    hostName = "localhost"
    port = "3306"
    rootPassword = "rootroot"
    general {
        user = "TESTUSER"
        password = "PASSWORD"
        db = "TESTDB"
        baseUrl = "jdbc:mysql://localhost:${port}"
        url = "${baseUrl}/${general.db}"
        connPoolName = "MySQLPool"
        datasourceJndiName = "jdbc/MySQLDS"
    }
    sales {
        user = "USER_S"
        password = "PASSWORD"
        db = "DB_S"
        baseUrl = "jdbc:mysql://localhost:${port}"
        url = "${baseUrl}/${general.db}"
        connPoolName = "MySQLSalesPool"
        datasourceJndiName = "jdbc/MySQLSalesDS"
    }
}

// Derby
derby {
    derbyDir = "D:/Java/db-derby-10.13.1.1-bin"
    derbyDBDir = "C:/tmp"
    driverDir = "${derbyDir}/lib"
    driverJar = "derbyclient.jar"
    driverClass = "org.apache.derby.jdbc.ClientDriver"
    datasourceClass = "org.apache.derby.jdbc.ClientDataSource"
    hostName = "localhost"
    port = "1527"
    general {
        user = "TESTUSER"
        password = "PASSWORD"
        db = "TESTDB"
        url = "jdbc:derby://${hostName}:${port}/C:Users/kenya/${general.db};create=true"
        connPoolName = "DerbyPool2"
        datasourceJndiName = "jdbc/DerbyDS"
    }
}

// ActiveMQ
activemq {
    activemqDir = "D:/Java/apache-activemq-5.12.0"
    startCommand = "${activemqDir}/bin/win64/activemq.bat"
}

// JAX-WS
wsgen = "${System.properties['java.home']}/../bin/wsgen.exe"
wsimport = "${System.properties['java.home']}/../bin/wsimport.exe"